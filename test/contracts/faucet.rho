new result, print(`rho:io:stdout`) in {
    @"Faucet"!("fake", "UserA", *result) |
    @"Faucet"!("fake", "UserB", *result) |
    @"Faucet"!("fake", "UserC", *result) |
    @"Faucet"!("fake", "UserA", *result) |
    for (@(success, msg) <= result) {
        print!(msg)
        /* expect: (unordered)
            Faucet success, newcomer: UserA
            Faucet success, newcomer: UserB
            Faucet success, newcomer: UserC
            UserA has been registered
        */
    }
}
